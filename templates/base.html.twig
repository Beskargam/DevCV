<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %}
            <link href="https://cdnjs.cloudflare.com/ajax/libs/open-iconic/1.1.1/font/css/open-iconic-bootstrap.min.css"
                  rel="stylesheet">
            <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css">
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}
    </head>
    <nav class="navbar navbar-expand-lg navbar-light">
        <a class="btn navbar-brand mr-auto p-2" href="{{ path('app_index') }}"><img src="{{ asset('images/logo.png') }}"
                                                                                       style="height: 50px; width: 50px"></a>
        {% if app.user.username is defined %}
            <a class="nav-link p-2" href="{{ path('security_logout') }}">Déconnexion</a>
        {% endif %}
    </nav>
    <body class="home-bg">
        {% block body %}{% endblock %}
    </body>
        <footer class="">
            <div class="container-fluid">
                <div class="row justify-content-center">
                    <div class="footer-text">
                        YesWeDev Postulation @ {{ 'NOW'|date('Y') }}
                    </div>
                </div>
            </div>
        </footer>
        {% block javascripts %}
            <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
            <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
            <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
            <script src="{{ asset('js/javascript.js') }}"></script>
        {% endblock %}
</html>
